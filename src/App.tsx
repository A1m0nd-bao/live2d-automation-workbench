'use client';

import { useEffect, useMemo, useRef, useState } from 'react';
import { Activity, ArrowUpRight, Bot, Check, ChevronRight, CircleHelp, Clock3, FileImage, FolderOpen, Layers3, MoreHorizontal, Pause, Play, Plus, Settings2, Sparkles, Upload, X } from 'lucide-react';

type StageState = 'done' | 'working' | 'queued' | 'blocked';
type PipelineStep = { name: string; description: string; state: StageState; output: string; tool: string };
type Task = { id: string; name: string; state: string; progress: number; updated: string };
type Panel = 'guide' | 'assets' | 'templates' | 'runs' | 'rules' | 'settings' | 'production' | null;

const initialSteps: PipelineStep[] = [
  { name: 'Persona Lock', description: '锁定角色特征与全身构图', state: 'done', output: 'base_v3.png', tool: '角色一致性 Skill' },
  { name: '状态图生成', description: '生成 5 个可驱动表情 / 待机状态', state: 'done', output: 'states_v2.zip', tool: '动作规划 Skill' },
  { name: '图层拆分', description: '将全身图拆为可绑定的 PSD 图层', state: 'working', output: 'seethrough_output.psd', tool: 'See-Through Worker' },
  { name: '图层质检', description: '检查遮挡、缺失层、透明边与嘴部层级', state: 'queued', output: 'qa_report.json', tool: 'QA 规则集' },
  { name: 'Cubism 工程导出', description: '自动映射骨架与网格，并导出可编辑的 Cubism 工程', state: 'queued', output: 'aira.cmo3', tool: 'Stretchy Studio · CMO3 Export' },
  { name: 'Cubism 编译与验收', description: '编译运行模型，验证口型、呼吸、转头与动作幅度', state: 'queued', output: 'aira.moc3 + model3.json', tool: 'Cubism Editor · 验收脚本' },
];
const seedTasks: Task[] = [
  { id: 'L2D-024', name: '霓虹机甲 · 艾拉', state: '执行中', progress: 48, updated: '刚刚' },
  { id: 'L2D-023', name: '花店店主 · 米娅', state: '待验收', progress: 92, updated: '24 分钟前' },
  { id: 'L2D-022', name: '探险家 · 沈舟', state: '已交付', progress: 100, updated: '昨天' },
];
const nav = [{ label: '工作台', icon: Activity }, { label: '角色资产', icon: FolderOpen, panel: 'assets' as Panel }, { label: '流程模板', icon: Layers3, panel: 'templates' as Panel }, { label: '运行记录', icon: Clock3, panel: 'runs' as Panel }];
const stateStyle: Record<StageState, { label: string; className: string }> = { done: { label: '已完成', className: 'status-done' }, working: { label: '运行中', className: 'status-working' }, queued: { label: '排队中', className: 'status-queued' }, blocked: { label: '需处理', className: 'status-blocked' } };
const makePipeline = () => initialSteps.map((step, index) => ({ ...step, state: (index === 0 ? 'working' : 'queued') as StageState }));

export default function App() {
  const [steps, setSteps] = useState(initialSteps); const [tasks, setTasks] = useState(seedTasks); const [isRunning, setIsRunning] = useState(true); const [showCreate, setShowCreate] = useState(false); const [selectedNav, setSelectedNav] = useState('工作台'); const [referenceFile, setReferenceFile] = useState<File | null>(null); const [taskName, setTaskName] = useState('未命名角色'); const [executorUrl, setExecutorUrl] = useState(''); const [panel, setPanel] = useState<Panel>(null); const [toast, setToast] = useState(''); const fileInput = useRef<HTMLInputElement>(null);
  const progress = useMemo(() => Math.round(((steps.filter((step) => step.state === 'done').length + (steps.some((step) => step.state === 'working') ? .45 : 0)) / steps.length) * 100), [steps]);
  useEffect(() => { setExecutorUrl(localStorage.getItem('morph.executorUrl') ?? ''); }, []); useEffect(() => { if (!toast) return; const timer = window.setTimeout(() => setToast(''), 3200); return () => window.clearTimeout(timer); }, [toast]);
  const advancePipeline = () => setSteps((current) => { const workingIndex = current.findIndex((step) => step.state === 'working'); if (workingIndex === -1) return current; return current.map((step, index) => index === workingIndex ? { ...step, state: 'done' } : index === workingIndex + 1 ? { ...step, state: 'working' } : step); });
  const toggleRun = () => { if (isRunning) { setIsRunning(false); setToast('流程已暂停，任务和产物状态保持不变。'); } else { setIsRunning(true); advancePipeline(); setToast('流程已恢复，并推进到下一阶段。'); } };
  const saveExecutor = () => { localStorage.setItem('morph.executorUrl', executorUrl.replace(/\/$/, '')); setPanel(null); setToast(executorUrl ? '执行器地址已保存。新任务将提交到该地址。' : '已清除执行器地址，任务仅保留在工作台中。'); };
  async function createTask() { const name = taskName.trim() || '未命名角色'; const id = `L2D-${String(Math.max(...tasks.map((task) => Number(task.id.slice(4))), 23) + 1).padStart(3, '0')}`; const newTask: Task = { id, name, state: executorUrl && referenceFile ? '提交中' : '待连接执行器', progress: 0, updated: '刚刚' }; setTasks((current) => [newTask, ...current]); setSteps(makePipeline()); setShowCreate(false); setSelectedNav('工作台'); if (!executorUrl || !referenceFile) { setToast(`${id} 已创建。选择参考图并连接 See-Through Worker 后即可真实执行。`); return; } try { const form = new FormData(); form.append('image', referenceFile); form.append('name', name); const response = await fetch(`${executorUrl.replace(/\/$/, '')}/jobs`, { method: 'POST', body: form }); if (!response.ok) throw new Error(); const job = await response.json() as { id?: string }; setTasks((current) => current.map((task) => task.id === id ? { ...task, state: '图层拆分中', progress: 8 } : task)); setToast(`任务已提交到 See-Through Worker${job.id ? `：${job.id}` : ''}`); } catch { setTasks((current) => current.map((task) => task.id === id ? { ...task, state: '提交失败', progress: 0 } : task)); setToast('无法连接执行器。请检查“设置”中的 Worker 地址与跨域配置。'); } }
  return <main className="app-shell"><aside className="side-rail"><div className="brand-mark" aria-label="Live2D 工坊"><span className="brand-dot" /><span>morph</span></div><button className="new-task" onClick={() => setShowCreate(true)}><Plus size={17} /> 新建任务</button><nav className="main-nav" aria-label="主导航">{nav.map(({ label, icon: Icon, panel: target }) => <button key={label} onClick={() => { setSelectedNav(label); if (target) setPanel(target); }} className={selectedNav === label ? 'nav-item active' : 'nav-item'}><Icon size={18} />{label}</button>)}</nav><div className="rail-footer"><button className="nav-item" onClick={() => setPanel('settings')}><Settings2 size={18} />设置</button><div className="user-chip"><span>BR</span><div><b>包添荣</b><small>创作空间</small></div><MoreHorizontal size={17} /></div></div></aside><section className="workspace"><header className="topbar"><div className="crumbs"><span>工作台</span><ChevronRight size={14} /><b>角色生成</b></div><div className="top-actions"><button className="help" onClick={() => setPanel('guide')}><CircleHelp size={18} /> 流程指南</button><button className="avatar" onClick={() => setPanel('settings')}>BR</button></div></header><div className="workspace-scroll"><section className="intro-row"><div><p className="eyebrow">LIVE2D AUTOMATION</p><h1>角色生成工作台</h1><p className="intro-copy">从参考图到可交付的 Live2D 模型，把不稳定的制作步骤变成可追踪、可验收的流水线。</p></div><div className="header-actions"><button className="ghost-button" onClick={() => setPanel('assets')}><FileImage size={17} />资产规范</button><button className="primary-button" onClick={() => setShowCreate(true)}><Plus size={17} />新建角色任务</button></div></section><section className="overview-grid"><article className="feature-card current-task"><div className="card-top"><div><span className="task-id">L2D-024</span><h2>霓虹机甲 · 艾拉</h2></div><span className="live-pill"><i />运行中</span></div><div className="character-stage"><div className="scan-lines" /><div className="character-silhouette"><div className="halo" /><div className="head" /><div className="torso" /><div className="arm left" /><div className="arm right" /></div><div className="stage-label">角色预览 · v0.3</div></div><div className="progress-line"><div className="progress-copy"><span>全流程进度</span><b>{progress}%</b></div><div className="progress-track"><span style={{ width: `${progress}%` }} /></div></div><div className="task-meta"><span><Clock3 size={15} />预计还需 8 分钟</span><span><Bot size={15} />{executorUrl ? '已连接执行器' : '未连接执行器'}</span></div></article><article className="feature-card stats-card"><div className="card-heading"><div><p className="eyebrow">本周概览</p><h2>生产节奏</h2></div><button aria-label="查看详情" onClick={() => setPanel('production')}><ArrowUpRight size={18} /></button></div><div className="stats"><div><strong>12</strong><span>创建任务</span></div><div><strong>8</strong><span>成功交付</span></div><div><strong>7.6</strong><span>平均质检分</span></div></div><div className="mini-chart" aria-label="近七日任务完成趋势"><span style={{height:'31%'}} /><span style={{height:'55%'}} /><span style={{height:'42%'}} /><span style={{height:'74%'}} /><span style={{height:'61%'}} /><span style={{height:'86%'}} /><span className="highlight" style={{height:'68%'}} /></div><div className="chart-labels"><span>周一</span><span>周二</span><span>周三</span><span>周四</span><span>周五</span><span>周六</span><span>今天</span></div></article></section><section className="pipeline-section"><div className="section-heading"><div><p className="eyebrow">PIPELINE</p><h2>自动化执行链路</h2></div><button className="run-button" onClick={toggleRun}>{isRunning ? <><Pause size={16} />暂停流程</> : <><Play size={16} />继续并推进</>}</button></div><div className="pipeline">{steps.map((step, index) => { const status = stateStyle[step.state]; return <article className={`pipeline-step ${step.state}`} key={step.name}><div className="step-number">{step.state === 'done' ? <Check size={15} /> : String(index + 1).padStart(2, '0')}</div><div className="step-content"><div className="step-title"><h3>{step.name}</h3><span className={status.className}>{status.label}</span></div><p>{step.description}</p><div className="step-footer"><span>{step.tool}</span><b>{step.output}</b></div></div></article>; })}</div></section><section className="bottom-grid"><article className="table-card"><div className="section-heading compact"><div><p className="eyebrow">RECENT RUNS</p><h2>最近任务</h2></div><button className="text-button" onClick={() => setPanel('runs')}>查看全部 <ChevronRight size={15} /></button></div><div className="task-table"><div className="table-head"><span>任务</span><span>状态</span><span>进度</span><span>更新时间</span></div>{tasks.slice(0, 3).map((task) => <button className="table-row task-link" key={task.id} onClick={() => setPanel('runs')}><div><b>{task.name}</b><small>{task.id}</small></div><span className={`task-state ${task.state === '已交付' ? 'delivered' : task.state === '待验收' ? 'review' : ''}`}>{task.state}</span><div className="row-progress"><span><i style={{width:`${task.progress}%`}} /></span><b>{task.progress}%</b></div><time>{task.updated}</time></button>)}</div></article><article className="risk-card"><div className="risk-icon"><Sparkles size={19} /></div><p className="eyebrow">今日建议</p><h2>优先处理嘴部图层</h2><p>现有测试中，口型张合与面部遮挡是最常见的失败点。将“嘴部 / 牙齿 / 口腔”设为拆层必检项。</p><button className="text-button" onClick={() => setPanel('rules')}>打开质检规则 <ArrowUpRight size={15} /></button></article></section></div></section>{toast && <output className="toast" aria-live="polite">{toast}</output>}{showCreate && <div className="modal-backdrop"><section className="create-modal" role="dialog" aria-modal="true" aria-labelledby="create-title"><button className="close-modal" onClick={() => setShowCreate(false)} aria-label="关闭"><X size={20} /></button><p className="eyebrow">CREATE PIPELINE RUN</p><h2 id="create-title">新建角色任务</h2><p className="modal-copy">接入 Worker 后，提交会真实触发 See-Through 的分层 PSD 任务。</p><label className="field-label">角色名称<input value={taskName} onChange={(event) => setTaskName(event.target.value)} /></label><label className="field-label">角色参考图<button className="upload-box" type="button" onClick={() => fileInput.current?.click()}><Upload size={21} /><span>{referenceFile?.name ?? '选择 PNG / JPG 图片'}</span><small>建议全身、正面或半身角色图</small></button><input ref={fileInput} className="hidden-input" type="file" accept="image/png,image/jpeg" onChange={(event) => setReferenceFile(event.target.files?.[0] ?? null)} /></label><div className="modal-actions"><button className="ghost-button" onClick={() => setShowCreate(false)}>取消</button><button className="primary-button" onClick={createTask}>创建并开始执行 <ArrowUpRight size={16} /></button></div></section></div>}{panel && <PanelDialog panel={panel} tasks={tasks} executorUrl={executorUrl} setExecutorUrl={setExecutorUrl} onClose={() => setPanel(null)} onSaveExecutor={saveExecutor} />}</main>;
}

function PanelDialog({ panel, tasks, executorUrl, setExecutorUrl, onClose, onSaveExecutor }: { panel: Exclude<Panel, null>; tasks: Task[]; executorUrl: string; setExecutorUrl: (value: string) => void; onClose: () => void; onSaveExecutor: () => void }) {
  const content: Record<Exclude<Panel, null>, { eyebrow: string; title: string; body: React.ReactNode }> = { guide: { eyebrow: 'PIPELINE GUIDE', title: '流程指南', body: <ol className="guide-list"><li>上传单一角色参考图，先生成全身统一底图。</li><li>把底图提交到 See-Through Worker，得到分层 PSD。</li><li>质检通过后导入 Stretchy Studio，导出 Cubism 工程。</li><li>在 Cubism Editor 中编译并验收 `.moc3`。</li></ol> }, assets: { eyebrow: 'ASSET SPEC', title: '资产规范', body: <ul className="guide-list"><li>输入：PNG / JPG，建议 ≥ 1280px，单角色、无遮挡。</li><li>中间产物：分层 PSD 与质检报告。</li><li>可编辑交付：`.cmo3`；运行时交付：`.moc3 + model3.json`。</li></ul> }, templates: { eyebrow: 'PIPELINE TEMPLATE', title: '默认流程模板', body: <div className="detail-stack"><b>See-Through → QA → Stretchy Studio → Cubism Editor</b><span>每个阶段均可独立重跑并保留上游产物。</span></div> }, runs: { eyebrow: 'RUN HISTORY', title: '运行记录', body: <div className="detail-stack">{tasks.map((task) => <div className="run-item" key={task.id}><span><b>{task.name}</b><small>{task.id}</small></span><em>{task.state} · {task.progress}%</em></div>)}</div> }, rules: { eyebrow: 'QA RULES', title: '图层质检规则', body: <ul className="guide-list"><li>嘴部、牙齿、口腔必须独立成层。</li><li>左右眼、虹膜与眼白需单独可见且无透明边。</li><li>前后发、手臂和服装遮挡关系必须通过深度校验。</li></ul> }, settings: { eyebrow: 'EXECUTOR CONFIG', title: '连接 See-Through Worker', body: <div className="detail-stack"><span>GitHub Pages 只负责界面。填入带 GPU 的 Worker 地址后，新建任务会真实提交图片进行 PSD 拆分。</span><label className="field-label">Worker 地址<input value={executorUrl} onChange={(event) => setExecutorUrl(event.target.value)} placeholder="http://localhost:8787" /></label><button className="primary-button" onClick={onSaveExecutor}>保存连接</button></div> }, production: { eyebrow: 'WEEKLY REPORT', title: '生产节奏', body: <div className="detail-stack"><b>8 / 12 个任务已交付</b><span>当前瓶颈是 PSD 图层质检。优先收集失败样本，完善嘴部、眼部和遮挡检查规则。</span></div> } };
  const item = content[panel]; return <div className="modal-backdrop"><section className="create-modal panel-modal" role="dialog" aria-modal="true" aria-labelledby="panel-title"><button className="close-modal" onClick={onClose} aria-label="关闭"><X size={20} /></button><p className="eyebrow">{item.eyebrow}</p><h2 id="panel-title">{item.title}</h2><div className="panel-body">{item.body}</div></section></div>;
}
